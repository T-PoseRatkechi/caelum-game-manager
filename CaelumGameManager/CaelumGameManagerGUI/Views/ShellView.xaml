<Window x:Class="CaelumGameManagerGUI.Views.ShellView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:CaelumGameManagerGUI.Views"
        mc:Ignorable="d" FontSize="18"
        Title="Caelum Game Manager" Height="450" Width="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="10" />
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="10" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="10" />
            <RowDefinition Height="auto" />
            <RowDefinition Height="auto" />
            <RowDefinition Height="auto" />
            <RowDefinition Height="auto" />
            <RowDefinition Height="auto" />
            <RowDefinition Height="10" />
        </Grid.RowDefinitions>
        
        <Button x:Name="LoadMusic" Grid.Column="1" Grid.Row="1">Build Music</Button>
        <TextBlock Grid.Column="2" Grid.Row="1" x:Name="Status"/>
        
        <ListBox x:Name="Packages" Grid.Column="1" Grid.Row="2" Grid.ColumnSpan="2"
                 ScrollViewer.CanContentScroll="True" MaxHeight="500">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <TextBlock Text="{Binding PackageType}" />
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
    </Grid>
</Window>
